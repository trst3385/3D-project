6/1
헌혈 134회
하루 휴식

- 5월의 치열했던 기억들을 되돌아보며, 몸과 마음을 정비하고 6월을 준비하는 휴식.
- 134회 헌혈을 했어. 처음엔 기념품 받을려고 한 두번 시작한게 벌써 6년하고 2달째야. 쭉 이어가보자! 200번 채워봐야지!



6/2
몬스터 프리팹에 World Space. UI 체력바를 부착하고, 실제 체력 데이터와 연동

- 구조 설계: UI Canvas를 World Space 모드로 설정하여 몬스터 자식으로 배치.
- 자동 연결: transform.Find를 사용하여 인스펙터 의존성 없이 런타임에 체력바를 찾도록 구현.
- 데이터 연동: 몬스터 체력 스크립트의 TakeDamage() 함수 내부에서 Slider.value를 실시간 업데이트하여 체력 감소 시각화 성공.

- 발생했던 러블슈팅:
   1. 초기값 오류: Awake() 호출 순서와 Start의 초기화 시점이 맞지 않아 발생한 문제. → Awake()에서 체력을 초기화 하지 않고 Start()에서 체력을 초기화한 후 슬라이더 값을 갱신하는 방식으로 수정
   2. 비율 문제: 체력바 Slider의 Max Value가 기본값(1)으로 고정되어 있어 몬스터의 체력값 10과 매칭되지 않음. → 인스펙터와 Awake에서 MaxValue = maxHealth로 강제 지정하여 해결
   3. UI 표현 문제: Fill Area와 Fill 오브젝트의 Anchor가 Stretch로 되어있지 않아 체력바가 비정상적으로 깎임. → Anchor를 Stretch로, Offset을 모두 0으로 재설정하여 해결

- 다음 단계: 
   1. 직접 구현해 보았기에 나중에 에셋 스토어의 체력바 에셋을 쓰더라도, 어떤 컴포넌트를 조작해야 하는지 훨씬 빨리 이해할 수 있을거야
   2. 유니티 UI 시스템의 원리: 슬라이더의 Max Value와 Fill Area의 Anchor 설정은 UI 시스템의 기본 중 기본이야, 오늘 이를 통해 UI 렌더링 원리를 확실히 깨달았지
   3. 지금은 몬스터 오브젝트의 자식으로 체력바 UI가 들어가 있지만 나중에 개발 규모가 커질때 다른 방식 혹은 옵저버 패턴으로 고려해보자



6/3
플레이어 체력 시스템, 이동 로직 최적화
타워 공격 시스템 개선

- PlayerHealth 스크립트, 플레이어의 UI 체력바 추가 및 체력바(슬라이더)와 연동. 몬스터가 마지막 웨이포인트(Waypoints5)에 도달하면 플레이어의 체력을 깎고 몬스터를 삭제하는 로직 구현.

- EnemyMovement 스크립트의 Update() 함수에 있던 이동 로직을 별도의 함수 MoveToWaypoints()로 분리하여 코드 가독성 및 유지보수성 향상.
- OnReachedEnd() 함수를 추가해 몬스터 오브젝트가 마지막 포인트(Waypoints5)에 도달하면 오브젝트는 삭제하고 플레이어에게 데미지를 주는 로직 추가.

- AttackTower 스크립트 내 사거리 안에 몬스터를 식별하는 방식인 Physics.OverlapSphere(프레임 부하가 큼) 방식을 OnTriggerEnter/Exit 기반의 리스트 관리 방식으로 변경.
   1. 이를 위해 몬스터 오브젝트에 Rigidbody를 추가(Use Gravity를 체크 해제, Is Kinematic을 체크)하여 엔진의 감지 정확도를 확보 (근데 솔직히 차이는 못느꼈어)
   2. 리스트(List<Transform>)를 활용해서 사거리 내 적을 관리하는 효율적인 로직 적용
      2-1. `몬스터가 사거리 내에 있는데도 인식을 못 하는 문제` 가 있어서 당장은 인스펙터에 보이도록 유지, 
      만약 리스트를 숨겨놓으면, "적이 사거리에 들어왔는데 도대체 왜 안 쏴?" 라고 고민할 때 이게 타워 문제인지, 몬스터 태그 문제인지, 아니면 리스트가 아예 비어있는 건지(사거리 안에 들어와도 감지를 못함) 알 방법이 없으니까


- 추가로 겪은 문제와 해결 과정.
   1. FindObjectOfType 사용 시 발생하는 경고 메시지
      1-1. 해결: 최신 유니티 권장 방식인 Object.FindFirstObjectByType으로 수정
   2. 몬스터 속도가 비정상적으로 빠를 때 물리 연산보다 몬스터의 이동이 앞서서 발생하던 문제
      2-1. 기술 부채: 몬스터 속도가 비정상적으로 매우 빠를 때 사거리에 들어와도 공격을 하지 못해. 그래서 추후에 프레임 기반의 연산 최적화나 그 외의 방법을 동원해서 해결 예정



6/4
기존까지 하드코딩된 모든 스크립트들을 데이터 중심 설계(Data-Driven Design)로 리팩토링, ScriptableObject(SO)를 활용해 데이터와 로직을 완전히 분리.
타워의 사거리 설정 및 관리 방식의 개선(SphereCollider의 Radius 방식에서 SO의 range 변수값으로 변경)

- 데이터 관리: EnemyData, TowerData, PlayerData를 각각 ScriptableObject로 생성.
- 특히 몬스터(빨간 큐브)는 EnemyMovement -> NormalEnemy으로 이름 변경. 이 스크립트 중심축으로 삼아 몬스터의 데이터를 관리.
   1. NormalEnemy 스크립트에 EnemyData So를 연결
   2. EnemyHealth 스크립트는 NormalEnemy에 연결된 So를 받아 체력값 설정
- 나무(AttackTower) 및 발사체(Bullet) 데이터 적용:
   1. TowerData SO를 생성하여 타워 자체 값 외에도 발사체의 속도, 사거리, 발사체 스탯 등을 통합 관리
   2. 다만, 발사체(Bullet)는 타워와 물리적으로 분리된 별개의 오브젝트라서 프로퍼티 참조 방식이 아닌 '생성 시점의 데이터 주입' 방식을 채택
   3. AttackTower가 Instantiate 직후 Bullet.Initialize() 함수를 통해 타워가 가진 SO 데이터를 직접 전달함으로써, 발사체가 타워의 생명주기에 의존하지 않고 독립적으로 동작하도록 설계
   4. 발사체의 결합도(Coupling)를 낮추고, 만약에 타워가 파괴 되어도 발사체가 안전하게 동작할 수 있는 구조를 확보
   5. 즉, 타워와 발사체는 별개의 오브젝트지만 몬스터가 사거리 안에 들어오면 발사체는 생성되자마자 독자적으로 값 받은 상태로 동작하니까 타워의 **생명주기**에 대한 의존도 없이 독자적으로 행동

**생명주기(Life Cycle)란?: 용어는 프로그래밍, 특히 유니티 같은 게임 엔진에서 아주 중요한 개념이야. 쉽게 말해 "오브젝트가 태어나서부터 죽을 때까지 겪는 일련의 과정"을 뜻해**
**즉, 생명주기가 서로 다른 오브젝트들이 서로의 존재에 의존하지 않게 만드는 것(독립성), 이게 바로 프로그래밍에서 말하는 '안전한 설계'의 핵심이야**


- 기존에는 인스펙터에서 SphereCollider의 Radius 값을 직접 수정해서 사거리를 조정했지만 SO를 도입했기에 TowerData SO에 range 변수를 별도로 선언.
- 우선순위 결정 및 자동화:
   1. AttackTower 스크립트의 Start() 함수에서 col.radius = towerData.range; 코드를 호출함으로써, 에디터 상의 Radius 값보다 SO의 range 변수가 항상 우선하도록 강제
   2. 이를 통해서 인스펙터에서의 Radius는 개발자가 타워의 사거리를 시각적으로 확인하기 위한 '가이드라인'으로 활용하고, 실제 게임 로직상의 사거리는 SO 파일 하나로 통합 관리하는 구조를 확립
   3. 콜라이더를 0으로 두지 않고 시각적 가이드를 유지함으로써 디자인 편의성을 챙기는 동시에, 게임 실행 시점에는 SO의 값만을 받아 정확한 데이터값이 적용되도록 하여 '데이터 무결성'과 '생산성' 을 동시에 확보한거야



6/5
타워 이름 변경(AttackTower->AttackTree)
공격 타워 반응성 개선. 사거리 내 들어오면 즉시 공격
슬로우 타워 구현

- 이름은 Tower. 타워라곤 하지만 보기에는 나무 오브젝트이기에 (포트폴리오 작품의 개발 기간과 외형보단 기능과 로직에 더 집중하기 위해) 타워를 나무로 이름을 변경.

- AttackTree 스크립트 내 InvokeRepeating 방식의 지연 문제를 해결하기 위해 Update와 fireCountdown 변수를 활용한 쿨타임(Cooldown) 제어 로직을 구현. 타겟 감지 즉시 첫 공격이 수행되도록 반응성을 최적화.

- AttackTree와는 별개의 SlowTree스크립트를 구현하여 타워별 기능 분리(SRP 준수)를 달성.
   1. 몬스터 이동 속도 제어 시스템: 몬스터(NormalEnemy) 스크립트에 슬로우 상태 이후 원래 속도 보존용BaseSpeed와 실제 적용되는 실시간 적용된 속도CurrentSpeed를 추가. 슬로우 디버프 적용 및 타워 범위 이탈 시 정상 속도로의 복귀 로직을 안정화

- 데이터 주도 설계: 모든 타워 설정을 ScriptableObject 기반으로 관리하도록 계획. 코드 수정 없이 유니티 에디터에서 데이터 파일(SO) 수정만으로 사거리, 공격 속도, 슬로우 비율 등을 실시간 조절 가능한 유연한 구조를 확보.
   1. SlowTree를 첫 구현때는 AttackTree의 상속을 받으려 했으나 AttackTree는 여럿 타워 중 하나일 뿐, 중심이 되는 오브젝트가 아니기게 현재의 각자의 스크립트로 분리


**각 타워마다 사거리가 다를 수 있으니 각자의 SO를 추가하던지, 스크립트에서 SO값을 우선으로 처리, 혹은 So의 값을 쓰지 않고 스크립트에서 개별로 값을 추가**



6/7
3D 작품의 Git 레포지토리 추가(Git DeskTop으로)

- 어제 6일엔 알바를 다녀와서 아무것도 못했어.....그리고 오늘은 새벽까지 하고 와서 많이 피곤해서...변명인거 알지만 그래도 아무것도 안할 순 없어서 레포지토리 추가만이라도 했어.

- 환경 구축: 내 3D 프로젝트의 효율적인 버전 관리를 위해 기존의 2D 작품의 레포지토리가 아닌 새 Git 레포지토리를 새로 생성하고 원격 저장소(GitHub)와 연동.
   1. Git DeskTop으로 사이트에서 만드는 것보다 더 편리하게 만들었어
   2. 내 3D 프로젝트에서는 당연하게 체계적인 형상 관리와 데이터 주도 설계(SO)를 기반으로, 확장성이 뛰어난 타워 디펜스 아키텍처를 구현해 나갈 계획이야 2D 작품으로 배웠던 1년 덕분에 더 효율적이고 빠른 개발이 될 수 있어



6/8
LineRenderer를 활용하여 타워와 타겟 몬스터를 연결하는 조준선을 구현

- 타워가 적을 공격할 때, 사용자가 현재 어느 몬스터를 공격 중인지 직관적으로 알기 어려워 피드백이 부족해보였어. 
- 그래서 처음 사용해보는 LineRenderer 컴포넌트를 활용해 타워와 타겟 몬스터를 실시간으로 연결하는 시각화 로직 구현.
   1. AttackTree 스크립트의 Update() 함수 내에서 타겟이 존재할 경우 lineRenderer.enabled = true로 활성화
   2. Size는 2로 설정하고, SetPosition을 사용하여 0번 인덱스는 firePoint.position(조준선의 첫번째 위치), 1번 인덱스는 target.position(대상 몬스터의 위치)으로 매 프레임마다 업데이트
      2-1. LineRenderer 컴포넌트의 Size란?: Size가 2라면: [시작점] → [끝점] 딱 한 구간만 그어져. (내가 지금 하는 방식), Size가 3이라면: [시작점] → [중간점] → [끝점] 이렇게 꺾인 선을 그릴 수 있어
      2-2. SetPosition란? LineRenderer가 "어디서부터 어디까지 선을 그릴지" 그 좌표(Position)를 직접 지정해 주는 함수야, SetPosition은 라인의 각 점들을 내가 원하는 좌표로 실시간으로 이동시키는(갱신하는) 명령어라고 생각하면 돼)
         2-a. lineRenderer.SetPosition(0, firePoint.position);//0번 점(시작점)을 타워의 발사 위치로 옮겨!
         2-b. lineRenderer.SetPosition(1, target.position);   //1번 점(끝점)을 몬스터의 현재 위치로 옮겨!
         2-c. 만약 SetPosition을 안 쓰면 어떻게 될까? LineRenderer는 처음에 설정된 그 고정된 위치(아까 본 월드 중심 0,0,0)에만 선을 그려. 몬스터가 아무리 움직여도 선은 꼼짝도 안 하겠지, 
         하지만 Update() 함수 안에서 매 프레임 SetPosition을 호출하면 SetPosition(1, target.position) 코드가 몬스터의 새로운 좌표를 읽어와서 라인의 끝점을 그곳으로 이동시키고 마치 선이 몬스터를 계속 따라다니는 것처럼 보이는 거지
   3. 타겟이 사라지거나 범위 밖으로 나가면 else { lineRenderer.enabled = false; } 로 비활성화하여 리소스 낭비 방지



6/11
9~10일 본가에 다녀옴
몬스터 처치 시스템 구현 및 클리어 조건 로직 완성

- GameManager 싱글톤 시스템 스크립트 구축.
   1. 씬 전역에서 접근 가능한 GameManager를 통해 게임 데이터(처치 수, 총 몬스터 수) 관리
   2. 동적 UI 자동 연결 시스템
      2-1. GameObject.Find와 transform.Find를 조합하여 인스펙터 수동 연결 없이, 이름만으로 UI 컴포넌트(WaveText, GameClearPanel)를 자동 연결
      2-2. 특히, 비활성화된 UI 패널인 GameClearPanel을 transform.Find로 찾아내는 로직을 통해 씬 구성의 효율성 확보

- 게임 승리 판정 및 연출.
   1. 몬스터 처치 시 EnemyHealth 스크립트의 Die() 함수에서 GameManager 스크립트의 OnEnemyDefeated()에게 신호를 보내 수신하여 WaveText UI 업데이트	
   2. 승리 조건(10/10) 달성 시 코루틴(IEnumerator)을 사용하여 1초 뒤 GameClearPanel을 출력하고 게임 시간을 정지(Time.timeScale = 0f)시키는 연출 구현

- GameManager에서 발생한 트러블슈팅.
   1. 이슈: GameObject.Find를 사용하니 비활성화된 오브젝트를 찾지 못하고 경고가 발생
   2. 해결: GameObject.Find 대신 부모인 Canvas를 먼저 찾은 후, transform.Find를 사용하여 비활성화된 자식 오브젝트까지 정확히 참조하도록 구조 개선



6/12
게임 내 일시정지(Pause) 시스템 구현 및 UI 피드백 적용

- 싱글톤패턴 기반의 PauseManager 스크립트 구현.
   1. 씬 어디서든 호출 가능한 Instance를 통해 일시정지 상태를 관리
   2. Awake()에서 중복 오브젝트를 제거하는 질서 유지 로직 구현
- 일시정지 창의 PausePanel UI 구현.
   1. 당장은 일시정지의 목적이라 수동 버튼 클릭 목적의 ResumeButton 버튼UI만을 구현

- 입력 시스템(Input System) 대응.
   1. 최신 Input System Package 환경에서 Keyboard.current를 활용한 Esc 키 입력 감지 로직 적용

- 일시정지 로직.
   1. Time.timeScale을 이용해 일시정지 시 게임 시간을 멈추고, 다시 Esc나 버튼을 클릭 시 복구하는 기능 구현
   2. activeSelf를 뒤집는(Toggle) 방식으로 하나의 함수로 창을 켜고 끄는 로직 완성!

- UI 피드백 (버튼 클릭 시 색상 변화).
   1. ResumeButton의 인스펙터에 Transition(Color Tint) 설정을 통해 마우스 호버(Highlighted) 및 클릭(Pressed) 시 색상 변화 구현
   2. Highlighted와 Pressed 색상을 동일하게 설정해서 시각적 안정감 부여 (현재는 노란색으로)

- 트러블슈팅.
   1. 문제: 2D작품에서 사용했던 Input System 패키지 사용 중 Input.GetKeyDown 사용으로 인한 오류 발생
   2. 해결: using UnityEngine.InputSystem;, Keyboard.current.escapeKey.wasPressedThisFrame 을 사용하여 패키지 표준 방식에 맞게 코드 수정 및 안정성 확보
      2-1. if (Keyboard.current != null && Keyboard.current.escapeKey.wasPressedThisFrame) TogglePause();



6/13
PauseManager 스크립트의 UI 연결 방식 최적화 및 안정성 확보

- GameObject.Find를 통한 자동 연결 방식이 비활성화된 UI 오브젝트를 탐색하지 못하는 이슈 발생!!
   1. 원인: 유니티의 GameObject.Find는 씬에 활성화된 오브젝트만 탐색 가능하다는 제약 사항이 있음 
   2. 아마 어제 난 PausePanel을 인스펙터에 드래그 연결을 한 상태에서 실행을 했기에 문제가 없다고 판단했나봐
- 오류가 뜨지 않았던 이유?: GameObject.Find("이름")로 비활성화된 UI를 찾을때 
   1. 찾기 시도: 유니티가 씬을 뒤져서 "PausePanel"이라는 이름을 찾으려고 해
   2. 못 찾음: 비활성화된 오브젝트라서 검색 결과에 포함되지 않아. 그럼 GameObject.Find는 null을 반환해
   3. 조용한 실패: 유니티는 null을 반환하고는 아무 일도 일어나지 않아. 오류를 내지 않는 이유는 "이름이 없는 걸 찾으라고 했다고 해서 그게 당장 프로그램이 아예 멈추진 않아" 라고 판단하기 때문이었어
   4. 그다음: 코드의 if (panelObj != null) 부분에서, panelObj가 null이니까 if 블록 안으로 들어가지 않았던 거야
   5. 즉, 오류가 안 난 게 아니라, if (panelObj != null)로 방어 코드를 짜두었기 때문에 코드가 안전하게 그냥 '건너뛴' 거지!!	
      5-1. Start() 안에서: panelObj가 null 상태니까 pausePanel 변수에 null이 할당되고, SetActive(false) 명령은 실행조차 안 됐어
      5-2. TogglePause() 안에서: pausePanel이 null이니까 if (pausePanel == null) return; 이 조건문에 걸려서 아무것도 안 하고 함수가 종료된 거지 내 코드가 오류를 알리지 말라고 한거야!

- 비활성화 객체 탐색 로직으로의 변경.
   1. Start() 함수에서 Resources.FindObjectsOfTypeAll<Canvas>()를 사용하여 씬 내의 캔버스를 탐색
   2. Transform.Find를 통해 비활성화 상태인 PausePanel을 직접 탐색하여 연결하도록 로직 변경
- 자동 연결 구조 완성:
   1. 인스펙터에서의 수동 드래그 앤 드롭 방식(하드코딩된 참조)을 **절대** 지양하고, 시스템이 런타임에 오브젝트를 스스로 찾아 연결하는 자동화 구현
   2. 씬 로드 및 초기화 과정에서 UI 탐색 로직이 안전하게 동작하도록 구조화

- 오늘의 트러블슈팅: GameObject.Find의 사용으로 인한 비활성화 된 UI 찾기 실패
   1. 원인: GameObject.Find가 비활성화된 객체를 찾지 못하니 NullReference 오류 발생
   2. 해결: Resources.FindObjectsOfTypeAll를 활용해 비활성화 상태의 자식 오브젝트까지 탐색 범위 확대하여 해결

- '자동 연결'이라는 편리함을 얻기 위해서는 유니티의 오브젝트 탐색 원리를 깊이 이해해야 해. 2D작품때도 고생했던 문제였는데도 말이지....GameObject.Find와 같은 기본 함수들의 제약 조건을 정확히 숙지해둬
- 자동 연결 로직을 구현하는 과정에서 코드 양은 늘어났지만, 시스템의 신뢰성은 크게 향상된거야. 오늘 아무것도 안한거랑 다름이 없다고 느껴졌지만 사실상 '자동 연결' 실패 상태를 바로 찾아내 버그를 수정한 큰 일을 한거야!



6/14
README에 `비활성화 UI 탐색 및 자동 참조 시스템 개선` 문제 해결 기재

- 어제 수정 완료했던 비활성화된 UI 찾기에 대해 적었어. 2D작품의 README의 문제해결 과정과 비슷하게 적었거든.
 
- 문제 상황:
   1. 기존 GameObject.Find() 방식은 비활성화된 오브젝트 탐색이 불가능하여 PausePanel 참조에 실패
   2. 수동 인스펙터 참조 방식은 씬마다 연결을 반복해야 하는 유지보수성 문제와 참조 누락(NullReference) 위험이 큼

- 분석 및 해결 과정.
   1. Resources.FindObjectsOfTypeAll<Canvas>()를 통해 비활성 객체까지 탐색 범위 확장
   2. scene.name 필터링으로 프리팹이 아닌 실제 씬 인스턴스만 정확히 선별
   3. Transform.Find("PausePanel")로 자식 UI를 찾아 동적 할당하여 하드코딩된 참조를 제거

- PauseManager 스크립트의 Start()함수의 핵심 코드를 추가.

- 회고 및 향후 과제.
   1. 유니티 API 활용: 모든 탐색 방식에는 장단점이 있으므로, 상황에 맞는 API를 선택하는 안목이 중요
   2. 방어적 설계: 에러를 숨기는 코드(if null)보다 에러를 빠르게 인지하고 해결하는 디버깅 습관이 필요
   3. 방법론의 한계 인식: 현재 방식은 프로젝트 규모가 커지면 이름 의존성으로 인해 한계가 있다. 향후 프로젝트 확장 시 DI(의존성 주입) 패턴이나 Tag/Layer 시스템으로 구조를 고도화할 계획
- 개발일지는 이렇게 [내가 겪은 문제 -> 해결 논리 -> 코드 증거 -> 스스로의 평가]가 들어있으면 나중에 3D 프로젝트를 완성했을 때, 너만의 훌륭한 '기술적 증거'가 될 거야.



6/15
135회 헌혈
다음 추가할 시스템 기획

- 135회 헌혈하고 왔어. 왼손잡이라서 오른쪽 팔에 주사를 꽂았는데 간호사님이 오른쪽 팔에 바늘 흉터도 많고 왼쪽 팔도 혈관이 잘 보여서 이젠 왼팔로 헌혈을 하려고 해.
- 헌혈을 하면 많이 피곤해져서 오늘은 개발은 하지 않고 일찍 끝낼려고 해. 그래도 최소한은 다음 내 3D작품에 추가할 시스템을 고안해봤어.

- (1) 나무(타워) 배치. 한 면에 줄줄히 세우는게 아닌 특정한 위치에만 배치하는 '배치 포인트 시스템'.
   1. 개발 효율성 (시간 대비 효과): 맵 전체를 그리드(Grid)로 관리하고, 모든 타일의 Placeable 여부를 계산하는 로직은 생각보다 예외 처리가 많아. 반면, 배치 포인트를 미리 정해두면 "이 위치에 타워가 있어, 없어?"만 확인하면 되니 로직이 훨씬 단순해져
   2. 난이도 밸런싱: 아무 곳에나 배치하게 하면 몬스터의 경로를 완전히 봉쇄하거나, 사기적인 배치를 할 가능성이 커져서 밸런스 잡기가 훨씬 어려워. 포인트를 제한하면, "개발자가 의도한 난이도" 안에서 플레이어가 고민하게 만들 수 있어
   3. 불안감 해소: 내가 걱정하는 '완성 기간'을 단축할 수 있는 가장 확실한 방법이야. 기능은 작게 시작해서 견고하게 만드는 게 최고겠지
   4. 배치 포인트 프리팹 만들때는 레이어를 나무보다 낮게 해서 나무보다 뒤로 가게 하는거 잊지말고!
      4-1. 코드 구현: 몬스터가 닿았을 때 반응했던 방식처럼, 마우스 클릭 시 Physics.Raycast를 사용해, 클릭한 지점이 TowerSlot 레이어라면 그 위치에 나무를 생성(Instantiate)하고, 해당 슬롯은 더 이상 클릭되지 않게 bool 값을 꺼버리면 돼 
      **이 방식은 아직 확정은 아니야!!**

- (2) 나무 강화. 특정 포인트(몬스터 처치 시 등장하는 골드 or 일정 수 이상 처치 or 특정 시간 경과).
   1. 시간이 지나거나 다음 라운드로 갈 수록 등장 몬스터는 강해질테니 나무도 강화를 시키자
   2. 나무의 외형을 바꾸거나, 아니면 단순히 나무의 크기나 색을 바꿔서 더 강해졌다고만 느껴질 수 있게

- (3) 나무를 파괴하려 시도하는 특정 몬스터(나무를 행동을 일시정지, 파괴).
   1. 몬스터가 강해졌다 해도 나무만 잘 키우면 재미 없겠지, 변수가 있어야 하니까
   2. 다르게는 몬스터가 마지막 포인트에 도달하지 않아도 플레이어 체력을 깎을 수 있게 한다거나
   3. 하지만 나무의 사거리에 닿지 않았는데 플레이어나 나무를 공격하는 몬스터가 공격을 하면 불리해지니 몬스터를 클릭or터치로 없애는 방법으로 해결해보자

**작품에 추가해봐!**
1. 나무(타워) 배치. 한 면에 줄줄히 세우는게 아닌 특정한 위치에만 배치하는 '배치 포인트 시스템'
2. 나무 강화. 특정 포인트(몬스터 처치 시 등장하는 골드 or 일정 수 이상 처치 or 특정 시간 경과)
3. 나무를 파괴하려 시도하는 특정 몬스터(나무를 행동을 일시정지, 파괴)



6/16
배치 포인트 시스템(TreeSlot) 기초 설계 및 환경 구성

- 오전에 먼저 취업 지원서 2곳 제출 완료했어. 이것만으로도 진이 다 빠지는거같네....

- 타워 디펜스 게임의 핵심인 '배치 포인트 시스템' 환경 구축. 정해진 자리에만 나무(타워)를 설치하게 할려는 목적이야.
- 이걸 사각형 맵에 줄줄히 나열해서 배치할지, 규모를 생각해서 한 라인에 4~5개만 고정적으로 배치할지는 다음에 생각해보자.
   1. Cube 오브젝트의 크기를 조절해 TreeSlot을 만듦.
   2. 배치를 위한 전용 Layer ('TreeSlot') 설정 및 레이어 마스킹 기반 마련
   3. 바닥면 밀착을 위한 Y축 위치 값 미세 조정 (Y=0.01).

- 리드 vs 포인트 방식: 시스템 복잡도와 개발 기간을 고려하여, 자유 배치 방식이 아닌 '배치 포인트(TowerSlot) 방식'으로 기획을 고려
- 추후 Physics.Raycast를 통해 클릭 지점이 TowerSlot 레이어인지 판별하여 나무를 생성하는 구조로 설계도 고려.

**오늘의 나에게: 어제 헌혈 후의 컨디션 저하와 취업 준비의 부담 속에서도, "어제는 헌혈 했고 오늘은 화요일이니 하지말까?" 쉴려는 생각보단 프로젝트를 놓지 않고 '기획'과 '기초 설계'를 완수한 책임감을 매우 칭찬해! 오늘 쌓은 기초는 내일의 코딩 효율을 극대화해 줄거야!**



6/17
배치 시스템 엔진 구현 및 시스템 구조 안정화

- 배치 시스템 엔진(PlacementManager) 구현: Raycast와 LayerMask를 활용해 특정 지점(TreeSlot)을 클릭 시 타워 배치 로직 완성.
   1. TreeSlot 상태 관리: isOccupied 변수를 통해 중복 배치 방지 로직 구현
   2. 현재는 TreeSlot에 오브젝트가 배치되는 것만 확인하려고 PlacementManager의 인스펙터에 public GameObject[] treePrefabs;에 나무 오브젝트를 연결해서,
   0번 인덱스의 나무만 고정으로 생성 되지만 훗날 나무를 선택해서 TreeSlot에 배치하는 방식으로 구현 예정

- 트러블슈팅 및 환경 설정:
   1. Unity Input System 충돌 해결 (Project Settings: Both 설정)
   2. 향후 UI 확장성을 고려해 PlacementManager 스크립트에 Debug.LogWarning 기반의 시스템 안내 로그 배치
      2-1. Debug.LogWarning($"[시스템 안내] 현재 {treePrefabs[currentSelectedTreeIndex].name} 배치 중. " + "나중에 UI를 만들어 나무 선택 기능을 구현하면 여기에서 인덱스(currentSelectedTreeIndex)를 동적으로 변경할 것!");

- 고민과 결정.
   1. 확장성: 처음에는 인스펙터에 할당된 인덱스의 고정된 타워 하나만 배치되지만, 추후 UI 버튼을 통해 다양한 타워를 선택할 수 있도록 GameObject[ ] 배열로 구조를 미리 잡아둔거야
   2. 배치 방식: 고민 끝에 TreeSlot 슬롯의 자유 배치보다는 고정된 TreeSlot슬롯 위치에서 나무를 배치 가능한 방식으로 개발 방향 확정. 이는 구현의 안정성을 높이고 빠른 개발 속도와 게임의 밸런스를 잡기에 더 효율적이라 판단

**내일은 Raycast와 LayerMask에 대해 집중적으로 배워보자. 지금은 당장 성과를 내야 해서 추가했지만 구체적으로 이게 뭔지 제대로 알아야 주도적으로 파악하고 구상할 수 있을테니**


6/18
나무 배치 UI 시스템 구축 및 PlacementManager와 연동

- UI 구축: Panel을 활용한 나무 선택창 배치 및 나무 이미지를 담은 아이콘(Image) 추가.
   1. 나무의 이미지는 프리팹을 캡쳐 후 프로젝트로 추가
   2. 이미지의 Texture Type을 Sprite (2D and UI)으로 변경, Sprite Mode를 Single로 변경 (Multiple로 되어 있으면 유니티가 이 이미지를 여러 개의 조각으로 쪼개서 인식하려고 해서 소스 이미지에 바로 안 들어갈 수 있기에)
   3. 이미지를 담은 Image에 Button 컴포넌트를 추가해 On Click()으로 PlacementManager 스크립트의 SelectTree() 함수 연결
- 로직 구현: PlacementManager 스크립트의 SelectTree(int index) 함수를 통해 클릭한 나무의 인덱스를 실시간으로 변경하는 구조 설계.
- 예외 처리:
   1. EventSystem.IsPointerOverGameObject()를 활용해 UI 클릭 시 맵 배치 로직이 작동하지 않도록 '클릭 관통' 버그를 방지
   2. TreeSlot 클래스에 isOccupied 상태 변수를 활용해서 이미 심어진 슬롯에 중복 배치되는 현상 방지

- PlacementManager의 EventSystem.IsPointerOverGameObject()란?: 
   1. 역할: 마우스가 UI 요소(이미지 버튼) 위에 있는지 확인
   2. 작동 원리: UI 위에 마우스가 있으면 true 반환, 없으면 false 반환
   3. 필요성: '클릭 관통' 방지. UI 버튼을 눌러 나무를 선택할 때, 만약 UI 뒤의 TreeSlot까지 동시에 클릭되는 현상을 차단
- 클릭 이벤트의 교통정리:
   1. UI 클릭 시 (true): return으로 코드 실행 중단. 맵 배치 로직(레이캐스트)으로 넘어가지 않음. → 선택 기능만 수행
   2. 맵 클릭 시 (false): '검문소' 통과. 레이캐스트를 발사하여 3D 공간의 TreeSlot을 찾아 배치. → 배치 기능만 수행

**즉, UI 버튼은 어떤 나무를 고를건지 '선택'만 담당하고, UI 밖의 게임 월드는 '배치'만 담당하도록 확실하게 정리를 해주는 거야**

- 레이캐스트(Raycast)란?:
   1. 한 줄 정의: 2D 화면상의 마우스 클릭을 3D 월드의 좌표와 물체로 변환해 주는 '탐지기'
   2. 없다면: 게임 월드의 어느 물체를 클릭했는지 구분할 수 없어 나무 배치가 불가능함


**다음에는 몬스터 이동 경로를 고려해 TreeSlot의 배치 수를 늘리는 '배치 및 타워 배치 구역 확장' 을 구안**



6/19
직접적인 코드 구현 없음 (생업 병행)

- 내일의 설계: 몬스터 경로를 따라 타워를 배치할 '슬롯 확장 로직' 구상 완료.
- 제 구현한 PlacementManager 스크립트의 레이캐스트와 IsPointerOverGameObject 로직을 다시 복기함. '몬스터 이동 경로의 나무 타워 배치를 위한 슬롯 추가'라는 다음 목표를 위한 알고리즘적 고민 시작.
- 오늘은 결국 코딩, 개발은 하지 않았지만, 내일 더 집중하기 위해 현실의 기반을 다지기로 했어. 난 분명 좋은 개발자가 될 사람이고 그렇게 되어야만 하니까....



6/20
TreeSlot 추가 배치 및 타워 배치 시스템 완성 및 로직 안정화

- 맵의 전략적 요충지(꼭짓점 4곳)에 TreeSlot 프리팹을 추가 배치 및 레이어(TreeSlot) 설정 완료.

- 배치 로직 구현:
- PlacementManager 스크립트를 통해 레이캐스트로 슬롯을 감지하고, 선택된 나무 프리팹을 배치하는 시스템 구축.
- UI 창에서 나무 버튼 선택 시 배치 모드가 활성화(isTreeSelected = true)되도록 로직 수정.
- 나무 배치 후 자동 배치 모드 종료(isTreeSelected = false) 로직을 추가하여 연속 배치 오류 수정.
   1. 이로써 배치 후, 선택하지 않은 상태에서 TreeSlot을 클릭하면 나무가 배치되는 오류를 수정

- 예외 처리: TreeSlot 스크립트의 isOccupied 변수를 활용한 중복 배치 방지 기능 구현.
   1. isOccupied = false (빈 땅): "여기는 나무가 없어! 나무 심을 준비됐어!" (배치 가능)
   2. isOccupied = true (점유된 땅): "야!!  여긴 이미 나무가 심어져 있어! 배치 못해!" (배치 불가)
   3. 이미 배치된 곳에 하나 더 배치?
      3-1. PlantTree() 함수가 실행될 때 isOccupied가 이미 true니까, return을 만나서 함수가 멈춰. 즉, 나무가 겹쳐서 배치되는 일은 절대 없지
   4. 이미 배치되어 있는데 클릭하면 다른 나무로 대체?
      4-1. 지금 로직으로는 대체되지 않아. 나무가 심어진 상태에서 클릭하면 return으로 막히니까 아무 일도 안 일어나지
      4-2. 만약에 나중에 "나무를 철거하고 새로 배치 하자"라는 기획을 넣고 싶다면, isOccupied를 false로 바꾸고 기존 나무를 파괴(Destroy)하는 로직만 추가하면 돼. ?아주 쉽지?



6/21
몬스터 이동 로직 분리 및 데이터 주도 설계(Data-Driven Design) 도입

- 이동 로직 분리: 기존 NormalEnemy 스크립트에 포함되어 있던 이동 로직을 EnemyMovement 컴포넌트(이동로직만을 담당하는 스크립트)로 분리.
- 컴포넌트 합성 방식 도입: 몬스터 스크립트는 EnemyMovement를 참조하여 이동하도록 수정하여 코드 재사용성 확보.
- 데이터 주도 설계 적용: EnemyData ScriptableObject를 활용하여 몬스터별 스펙(체력, 속도, 데미지)을 로직과 분리.
   1. 예시) 윈도우에서 `메모장.exe`(스크립터블 스크립트) 로 파일1.txt(EnemyData_SO) 와 파일2.txt(Enemy2_SO) 를 만들었을 때, 파일1의 내용을 수정한다고 파일2의 내용이 바뀌지 않는 것과 똑같아
      1-1. 내가 만든 EnemyData.cs는 '메모장 프로그램'이고, 유니티에서 생성한 여러 개의 .asset 파일들은 '저장된 텍스트 파일들'인 거지
      1-2. 쉽게 말하면 SO에셋은 여러개 생성하고 값을 서로 다르게 해도 틀이 되는 EnemyData.cs 자체는 바뀌지 않아. 새로 스크립터블 스크립트를 만들어서 SO를 완전 새로운걸로 만들 필요가 없어
- 확장 테스트: 큐브 오브젝트로 새로운 몬스터를 추가하여 동일한 이동 로직 스크립트로 다른 속도의 몬스터 구현 확인.

- 데이터와 로직을 분리하니, 새로운 몬스터를 추가할 때 스크립트를 수정할 필요 없이 SO 파일 생성만으로 해결되어 매우 효율적.
- 매번 프리팹에 컴포넌트를 직접 연결하는 반복 작업이 너무 초보자? 같고 번거로워서 추후에 '프리팹 베리언트'나 레이어 시스템을 통해 작업 효율을 더 높일 계획.
- 새로운 몬스터는 어떻게 스폰할지 고민해보자. 일정 몬스터를 처치 후? 아니면 마지막 등장 횟수에 등장하는 방식?



6/22
UIManager의 이벤트 기반 구조(Observer Pattern)로의 리팩토링

- 역할 분리: UIManager 스크립트에 혼재되어 있던 '게임 판정 로직'과 '클리어 창 UI 표시 로직'을 분리.
- 이벤트 기반 설계(Observer Pattern) 도입: GameManager를 상태 관리자로 설정하여 몬스터 처치 및 클리어 여부를 계산하고, 이를 Action 이벤트를 통해 외부로 알리도록 구현.
   1. UIManager가 GameManager의 이벤트를 구독하여 데이터를 갱신하도록 구조 변경 (결합도 감소)
   2. GameManager에서 처치할 몬스터를 다 잡았다고 확인되면 UIManager에게 신호를 보내 클리어 창 UI를 뜨게 하는거야
   3. 코드 안전성 향상: OnDestroy에서 이벤트 구독을 해제하여 메모리 누수 방지

- 이전까지는 내 나름대로 SRP에 맞춘다 했지만 결국 스크립트 간 의존성이 높아져서 구조가 복잡했는데, 옵저버 패턴의 이벤트를 활용하니 각 클래스가 자신의 역할에만 집중하게 되어 코드가 훨씬 깔끔해짐!
- 강한 결합은 최대한 줄여보자. 매니저 스크립트에도 있는 하드코딩도 SO를 바꿔볼까?
- 

**추후에 UIManager에 있는 WaveText(화면에 몬스터 처치 수를 알려주는 UI) UI를 따로 분리하자, 또 UIManager의 이름도 바꾸고...이 스크립트는 몬스터를 전부 처치 후 뜰 UI창이야. 여기에 다음 라운드로 가거나 게임 종료 버튼을 추가할테니**
**EnemyHealth의 Die() 함수에서 몬스터 사망 시 GameManager로 신호를 보내는 로직도 손보자. 하드코딩 상태야**




**나중에 3D작품에 적을 내용**
1. 중심 스크립트 하나에 SO를 적용하고 컴포넌트로 같이 연결된 다른 스크립트는 이 중심 스크립트에 연결된 SO를 받아오기. (유지보수 향상 목적, 한 스크립트에서만 수정하면 되는 편리함)
2. 나열한 기술들(싱글톤, 옵저버, 트랜지션 등)을 모두 다 넣으려고 하면 배가 산으로 가. 면접관은 "이 기술을 왜 썼는가?"를 궁금해해
   2-1인스펙터 드래그: "직관적이고 빠르게 값을 바꿀 수 있어서 썼습니다." (정답)
   2-2.싱글톤: "게임 전역에서 하나만 존재해야 하는 관리자(GameManager 등)가 필요해서 썼습니다." (정답)
   2-3. 옵저버 패턴: "UI와 로직을 분리하여 의존성을 줄이고 싶어서 썼습니다." (정답)
   2-4.포트폴리오에는 1~2개의 패턴만 확실하게 보여줘도 충분해. 오늘 내가 한 것처럼 데이터(SO)를 잘 활용하고, 의존성 주입(DI)을 이해하고 있다는 것만 보여줘도 신입 수준에선 베스트일거야!
3. 왜? 이런 방식의 작품을 만들었냐, 무슨 생각을 하면서 만들었냐를 적어보자
   3-1. 데이터의 외부화: 지금처럼 ScriptableObject로 데이터(수치)를 완벽하게 분리하고, "기획자가 엑셀로 데이터를 관리한다고 가정했을 때, 이를 어떻게 유니티에 불러올까?"를 고민해 봐
   3-2. 생명주기 관리 (Object Pooling): Instantiate와 Destroy를 계속 쓰면 메모리가 파편화돼. 총알을 100개 미리 만들어두고 껐다 켰다 하는 '오브젝트 풀링(Object Pooling)'을 적용해 봐. 이건 게임 개발자라면 반드시 물어보는 필수 질문이야!
   3-3. 예외 처리와 로깅: Try-Catch를 쓰거나, 데이터가 없을 때 Debug.LogError로 멈추는(Fail-fast) 로직. "예상치 못한 상황이 벌어졌을 때 프로그램이 죽지 않고 어떻게 로그를 남길 것인가"에 대한 고민
4. '왜' 결합도를 낮추려고 했는지 적어두기: "결합도를 낮추니 깔끔해졌다"는 결과도 좋지만, "결합도가 높으면 A 코드를 수정할 때 B 코드까지 건드려야 해서 수정이 번거롭고 복잡했는데 옵저버 패턴 등 이벤트 신호를 전하고 받는 식으로 수정하니,
   이젠 걱정이 사라졌다" 같은 감정을 한 줄 덧붙이면, 현업 개발자의 고충을 이미 경험하고 해결했다는 게 더 잘 느껴질거야!

